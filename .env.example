# Production Environment Variables for Vercel
# Copy these to your Vercel dashboard's Environment Variables section

# MongoDB Atlas connection string (replace with your actual connection string)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/zestyflow?retryWrites=true&w=majority

# Server port (Vercel handles this automatically)
PORT=5000

# JWT Secret (generate a strong secret for production)
JWT_SECRET=your-super-secret-jwt-key-here

# Client origin (replace with your actual domain)
CLIENT_ORIGIN=https://your-vercel-app-name.vercel.app

# Node environment
NODE_ENV=production
